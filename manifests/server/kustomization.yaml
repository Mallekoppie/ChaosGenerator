apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - secret.yaml
  - service.yaml
  - deployment.yaml

namespace: default

commonLabels:
  component: chaos-master

replicas:
  - name: chaos-master
    count: 1
