apiVersion: v1
kind: Service
metadata:
  name: chaos-master-service
  labels:
    app: chaos-master
spec:
  type: ClusterIP
  ports:
  - port: 9002
    targetPort: 9002
    protocol: TCP
    name: grpc
  selector:
    app: chaos-master
