apiVersion: apps/v1
kind: Deployment
metadata:
  name: target-http2
  labels:
    app: target-http2
    project: chaos-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: target-http2
  template:
    metadata:
      labels:
        app: target-http2
        project: chaos-generator
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: target-http2
        image: target-http:latest
        # `make kind-images` loads the locally built image into the node. Without
        # IfNotPresent the injected ":latest" tag defaults to Always, which makes
        # the kubelet try (and fail) to pull target-http from a registry.
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: https
          protocol: TCP
        - containerPort: 9090
          name: metrics
          protocol: TCP
        env:
        # Same image as the TLS target, switched to HTTP/2 by CHAOS_TARGET_PROTOCOL.
        - name: CHAOS_TARGET_PROTOCOL
          value: "http2"
        - name: CHAOS_TARGET_PORT
          value: "8080"
        - name: CHAOS_TARGET_METRICS_PORT
          value: "9090"
        - name: CHAOS_TARGET_CERT_FILE
          value: "/certs/tls.crt"
        - name: CHAOS_TARGET_KEY_FILE
          value: "/certs/tls.key"
        volumeMounts:
        - name: tls
          mountPath: /certs
          readOnly: true
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
            scheme: HTTPS
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
            scheme: HTTPS
          initialDelaySeconds: 5
          periodSeconds: 10
      volumes:
      - name: tls
        secret:
          secretName: target-tls
